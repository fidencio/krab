name: Deploy GitHub Pages

on:
  push:
    branches: [main]
  workflow_run:
    workflows: [Build PR preview]
    types: [completed]
  pull_request_target:
    types: [closed]
  workflow_dispatch:

permissions:
  actions: read
  contents: read
  pull-requests: write
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v5
        with:
          ref: main
          fetch-depth: 0
          path: source

      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: source/package-lock.json

      - name: Validate and build development site
        working-directory: source
        run: |
          npm ci
          npm run generate
          git diff --exit-code -- \
            package-lock.json charts/krab/Chart.yaml charts/krab/values.yaml \
            charts/precheck/Chart.yaml charts/precheck/values.yaml \
            Dockerfile.precheck upstream/precheck-image.lock.json \
            upstream/architecture.yaml README.md charts/krab/README.md \
            CHANGELOG.md src/generated
          npm test
          npm run test:chart
          npm run lint
          npm run build
        env:
          BASE_PATH: /${{ github.event.repository.name }}/dev/

      - name: Compose development site
        shell: bash
        run: |
          set -euo pipefail

          mkdir -p site/dev
          cp -R source/dist/. site/dev/

      # This job uses only scripts from main. PR artifacts are copied as static
      # files; none of their contents are run with this workflow's token.
      - name: Add current PR previews
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
        shell: bash
        run: bash source/scripts/compose-pr-previews.sh site

      - name: Add published release builders
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
        shell: bash
        run: bash source/scripts/compose-release-builders.sh site

      - uses: actions/upload-pages-artifact@v5
        with:
          path: site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-24.04
    needs: build
    steps:
      - uses: actions/checkout@v5
        with:
          ref: main
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v5
        # deploy-pages#434: bundled @actions/artifact still loads core punycode.
        env:
          NODE_OPTIONS: --no-deprecation

      - name: Link previews from pull requests
        if: success()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
          PAGES_URL: ${{ steps.deployment.outputs.page_url }}
        shell: bash
        run: bash scripts/comment-pr-previews.sh
